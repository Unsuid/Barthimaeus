# Local Tools - Safe Notes

These notes are intentionally redacted. They identify integrations without storing credentials.

- n8n runs locally on the Steam Deck and may be reachable on the LAN.
- A Samsung TV remote helper exists locally.
- Home Assistant integration exists locally.

Tokens, config files, URLs containing secrets, and device credentials must be recreated from Daniel's private local storage during restore. Do not store them here.
